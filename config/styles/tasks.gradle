task("vale",
        description: "Lint the project's markdown and text files with Vale.",
        group: "verification",
        type: Exec,
) {
    executable "vale"
    args "--no-wrap", "$rootProject.projectDir"
}
